let base, alt;

base = Number(prompt("Digite a base do retângulo:"));
alt = Number(prompt("Digite a altura do retângulo:"));

alert(`A área de retángulo, de base ${base}, e altura ${alt}, é ${base * alt}`)